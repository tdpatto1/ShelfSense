# ShelfSense Workspace

The completed Phase 2 deliverable is in `phase2/`.
The completed Phase 3 MVP deliverable is in `mvp/`.

Start with:

- `mvp/report.md` for the Phase 3 write-up
- `mvp/README.md` for setup and reproduction
- `mvp/app.py` for the MVP dashboard
